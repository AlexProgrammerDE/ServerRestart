name: ServerRestart
version: '${version}'
main: me.xginko.serverrestart.ServerRestart
authors: [ xGinko ]
description: '${description}'
website: '${url}'
api-version: '1.19'
folia-supported: true
load: POSTWORLD

commands:
  restarts:
    usage: /restarts [ reload, version, disable ]
    description: ServerRestart admin commands
    aliases:
      - sr